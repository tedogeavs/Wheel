pages:
  stage: deploy
  image: alpine:latest
  script:
    - mkdir public
    - cp index.html styles.css script.js public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
